

const FeatureCard = ({item}) => {
    const {image,description,title} = item;
    return (
        <div>

            <h2>{title}</h2>
            
        </div>
    );
};

export default FeatureCard;